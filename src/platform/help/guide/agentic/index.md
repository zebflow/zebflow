# Agentic Use

Zebflow is designed to be operated by humans and agents.

Agentic use in Zebflow means:

- pipelines that call agent nodes
- agents that inspect and modify projects through MCP
- operators using the console and platform surfaces

The key is to keep this cheap and predictable:

- one project
- one tool surface
- one canonical way to inspect, run, and patch things
