# Publish metadata for the blessed local-hub seed.
# The seeder publishes this package as the reserved `zebflow` publisher.
id: zebflow.ui-layout
version: 0.1.0
title: "Zeb UI Layout"
description: "Layout components: card, table, tabs, accordion, collapsible, scroll-area."
