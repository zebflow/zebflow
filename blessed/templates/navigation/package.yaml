# Publish metadata for the blessed local-hub seed.
# The seeder publishes this package as the reserved `zebflow` publisher.
id: zebflow.ui-navigation
version: 0.1.0
title: "Zeb UI Navigation"
description: "Navigation components: breadcrumb, pagination, toggle, toggle-group."
