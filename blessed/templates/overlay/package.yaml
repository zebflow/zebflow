# Publish metadata for the blessed local-hub seed.
# The seeder publishes this package as the reserved `zebflow` publisher.
id: zebflow.ui-overlay
version: 0.1.0
title: "Zeb UI Overlay"
description: "Overlay components: dialog, alert-dialog, sheet, drawer, popover, hover-card, tooltip, dropdown-menu."
