# Publish metadata for the blessed local-hub seed.
# The seeder publishes this package as the reserved `zebflow` publisher.
id: zebflow.ui-complex
version: 0.1.0
title: "Zeb UI Complex"
description: "Composite components: select, sonner, input-otp, calendar, data-table."
