# Publish metadata for the blessed local-hub seed.
# The seeder publishes this package as the reserved `zebflow` publisher.
id: zebflow.ui-primitives
version: 0.1.0
title: "Zeb UI Primitives"
description: "Form and input primitives: button, input, textarea, label, checkbox, radio-group, switch, slider."
