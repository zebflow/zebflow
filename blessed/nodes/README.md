Foundation nodes (the bundled/ composites in src/pipeline/nodes/bundled/) are part of
the n.* node set shipped by the binary, NOT hub packages. This directory is the future
home of blessed node bundle packages; it is deliberately empty today.
