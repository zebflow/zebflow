# Publish metadata for the blessed local-hub seed.
# The seeder publishes this package as the reserved `zebflow` publisher.
id: zebflow.codemirror
version: 0.1.0
title: zeb/codemirror
description: "CodeMirror 6 editor runtime. Rich code editing with syntax highlighting, autocompletion, and markdown support."
