# Mapserver and GIS

Zebflow includes map and GIS features so spatial apps do not need a separate map server for common work.

Use it for:

- GeoJSON
- GeoParquet
- map layers
- bbox queries
- map publishing
- live map data
- data-driven map styles

For large spatial data, prefer file-backed formats and conversion nodes instead of passing full datasets through JSON payloads.
