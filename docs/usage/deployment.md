# Deployment

Zebflow can run as a local app, a server process, or a distributed deployment.

Common paths:

- `zebflow` for local standalone use
- Docker for server use
- controller and office roles for larger deployments
- Kubernetes for production clusters

Start simple. Move to a distributed setup only when the project needs stronger isolation, multiple workers, or production operations.
