# Sekejap DB

Sekejap is the embedded multi-model database available to Zebflow projects.

Use it when you want project-local data without installing another database. It can support ordinary records, graph links, vectors, text search, and spatial-style project data.

External databases are still supported. Sekejap is the default local data layer so small projects, demos, and research prototypes can be useful immediately.
